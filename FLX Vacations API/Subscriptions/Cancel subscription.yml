info:
  name: Cancel subscription
  type: http
  seq: 3
  tags:
    - Subscriptions

http:
  method: POST
  url: "{{baseUrl}}/api/subscriptions/:id/cancel"
  params:
    - name: id
      value: 0000000000000000aaaaaaaa
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
