info:
  name: Robots
  type: http
  seq: 3
  tags:
    - Health

http:
  method: GET
  url: "{{baseUrl}}/robots.txt"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
