PORT=3000
# CORS allowed origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173,https://flxvacations.com

# Full URI (credentials inside) OR base URI + username/password below
MONGODB_URI=mongodb+srv://cluster.mongodb.net/vaca-api
MONGODB_USERNAME=
MONGODB_PASSWORD=
